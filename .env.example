# Used by `docker compose up` (see docs/SELF_HOSTING.md). Copy to .env and
# fill in real values before deploying — do not commit .env.
POSTGRES_USER=compliance
POSTGRES_PASSWORD=replace-me
POSTGRES_DB=compliance_platform
JWT_SECRET=replace-me-with-a-long-random-value
JWT_EXPIRES_IN=12h
